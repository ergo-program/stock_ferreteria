<% modal ||= false %>
<% remote = modal ? true : false %>

<%= form_for(Brand.new, remote: remote, html: {role: :form, 'data-model' => 'brand'}) do |f| %>

<section class="content invoice">
  <div class="row">
    <div class="col-md-10">

      <div class="form-group">
        <%= f.label :nombre %>
        <%= f.text_field :nombre, :class => "form-control" %>
      </div>
      <div class="form-group">
        <%= f.submit :class => "btn btn-primary" %>
      </div>
    <% end %>
    </div>
  </div>
</section> 