<section class="content invoice">
  <div class="row">
    <div class="col-md-10">
      <%= form_for(@motive, :html => { :role => "form" }) do |f| %>
        <% if @motive.errors.any? %>
          <div class="alert alert-danger alert-dismissable" role="alert">
            <button type="button" class="close" data-dismiss="alert" aria-hidden="true">x</button>
            <h4><%= pluralize(@motive.errors.count, "error") %> prohibited this motive from being saved:</h4>

            <ul>
            <% @motive.errors.full_messages.each do |msg| %>
              <li><%= msg %></li>
            <% end %>
            </ul>
          </div>
        <% end %>

      <div class="form-group">
        <%= f.label :descripcion %>
        <%= f.text_field :descripcion, :class => "form-control", autofocus:true %>
      </div>
      <div class="form-group">
        <%= f.submit :class => "btn btn-primary" %>
      </div>
    <% end %>
    </div>
  </div>
</section> 